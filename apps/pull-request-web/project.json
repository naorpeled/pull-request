{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pull-request-web",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/pull-request-web",
        "outputPath": "dist/apps/pull-request-web"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/pull-request-web"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "pull-request-web:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "pull-request-web:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "pull-request-web:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "pull-request-web:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/pull-request-web"],
      "options": {
        "jestConfig": "apps/pull-request-web/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/pull-request-web/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
